{
  "private": true,
  "scripts": {
    "dev": "concurrently -n f,b -c blue,green \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "echo 'Backend has no build step'",
    "deploy": "npm run deploy:backend",
    "deploy:backend": "cd backend && npm run deploy",
    "deploy:worker": "npm install && cd backend && npm install && npm run deploy",
    "deploy:frontend": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "postinstall": "npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^9"
  }
}
